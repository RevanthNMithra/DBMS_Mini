CREATE DATABASE IF NOT EXISTS college;
USE college;

CREATE TABLE IF NOT EXISTS teacher (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL
);

CREATE TABLE IF NOT EXISTS student (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    usn VARCHAR(20),
    sem INT,
    subject_code VARCHAR(20),
    subject VARCHAR(100),
    marks INT
);

-- Insert login user (plain text password)
INSERT INTO teacher (username, password) VALUES ('admin', 'admin123');



ALTER TABLE student 
ADD COLUMN marks_test1 INT,
ADD COLUMN marks_test2 INT,
ADD COLUMN marks_test3 INT;
